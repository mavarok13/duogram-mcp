{
  "name": "duogram",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dist:desktop": "pnpm build && pnpm --filter @duogram/desktop exec electron-builder --publish never",
    "dist:npm": "pnpm build && pnpm --filter @duogram/core pack --pack-destination release && pnpm --filter @duogram/mcp pack --pack-destination release",
    "smoke:install": "node scripts/smoke-install.mjs",
    "build": "pnpm --recursive --if-present run build",
    "check": "pnpm run format:check && pnpm run typecheck && pnpm run lint && pnpm run test && pnpm run build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "typecheck": "tsc --build --pretty false"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "22.20.1",
    "eslint": "10.8.1",
    "eslint-config-prettier": "10.1.8",
    "prettier": "3.9.6",
    "typescript": "6.0.3",
    "typescript-eslint": "8.67.0",
    "vitest": "3.2.7"
  }
}
